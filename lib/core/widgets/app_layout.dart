abstract final class AppLayoutMetrics {
  static const bottomNavigationBarHeight = 78.0;
  static const bottomNavigationBarBottomOffset = 12.0;

  // Keep the last scrollable item above the floating navigation bar.
  static const bottomNavigationContentPadding =
      bottomNavigationBarHeight + bottomNavigationBarBottomOffset;
}
